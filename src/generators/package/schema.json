{
  "$schema": "http://json-schema.org/schema",
  "$id": "Package",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the package.",
      "default": "my-package",
      "x-prompt": "What name would you like to use for the package?",
      "pattern": "^[a-zA-Z][^:]*$"
    },
    "displayName": {
      "type": "string",
      "description": "The display name of the package.",
      "default": "My Package",
      "x-prompt": "What display name would you like to use for the package?"
    },
    "assemblyName": {
      "type": "string",
      "description": "The name of the assembly definition.",
      "default": "MyCompany.MyPackage",
      "x-prompt": "What name would you like to use for the assembly definition?",
      "pattern": "^[a-zA-Z][^:]*$"
    }
  },
  "required": ["name", "displayName", "assemblyName"]
}
